public class BiggestNumber {
    public void bignum(integer a, integer b, integer c){
        if(a>b && a>c){
            System.debug('A is the biggest number');
        }
        else if(b>c){
            System.debug('B is the biggest number');
        }
        else{
            System.debug('C is the biggest number');
        }
    }
}